<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
  <!-- Yisi AI brand mark (single source of truth). The ring hole is created by
       OPPOSITE path winding (non-zero rule), which both SVG renderers and icon
       font tools honor — not `fill-rule="evenodd"`, which font generators drop.
       Replace this file and run `npm run refresh:icons`. -->
  <path fill="#000" fill-rule="nonzero"
    d="M12 1 L23 12 L12 23 L1 12 Z
       M12 5.4 L5.4 12 L12 18.6 L18.6 12 Z
       M12 9.4 L14.6 12 L12 14.6 L9.4 12 Z"/>
</svg>
